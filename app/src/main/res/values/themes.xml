<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EasEBudgetV1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.EasEBudgetV1" parent="Base.Theme.EasEBudgetV1" />

    <!-- Optimization: Splash Screen Theme for instant startup feeling -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#FFFFFF</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/easebudgetlogo</item>
        <item name="postSplashScreenTheme">@style/Theme.EasEBudgetV1</item>
    </style>
</resources>